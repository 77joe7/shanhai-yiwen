# Git 与修改日志管理规范

## 1. 基本原则

- `main` 分支只保留可打开、可复核的稳定状态。
- 开发开始和交付前必须检查 `git status --short --branch`，不得覆盖或混入他人已有修改。
- 每次实质修改必须同时更新 `CHANGELOG.md` 的“未发布”部分。
- 文档内版本、文件名版本、`VERSION`、Git 标签和修改日志必须一致。
- 不覆盖已发布版本文件；新版本使用新文件名，旧版本通过 Git 历史和标签保留。
- Word 临时文件、渲染图片、QA 缓存和本地日志不得提交。
- 禁止提交密钥、令牌、个人信息、生产环境凭证和未授权资产。
- 禁止对共享分支强制推送或改写已共享历史；确需处理时必须获得明确批准并先建立可恢复引用。
- 提交是版本记录，不等同于异地备份；至少推送到一个受控远程仓库后才形成独立副本。
- 同步盘、编辑器历史、未提交工作区、构建产物和临时压缩包均不得替代 Git 远程备份。

## 2. 版本号

采用 `主版本.次版本.修订号`：

- 主版本：改变产品边界、核心方向或不兼容的数据/存档契约。
- 次版本：增加系统、章节、重大规则或可见功能。
- 修订号：修正文案、排版、错误或不改变既有行为的小调整。

文档显示可使用 `V1.3`，仓库与标签使用完整版本 `1.3.0`。

## 3. 标准工作流

1. 开始前运行 `git status --short --branch`，确认分支、工作区状态和他人已有修改。
2. 修改前阅读当前 `CHANGELOG.md` 和相关受控文档。
3. 在“未发布”下同步登记新增、修改、修复或移除内容。
4. 完成适当验证；DOCX 必须更新目录并完成渲染检查。
5. 使用 `git diff --check`、`git diff --stat`、`git diff -- CHANGELOG.md` 和 `git status --short` 核对范围。
6. 按单一目的提交，避免把不相关修改混入同一提交。
7. 提交后检查提交内容和工作区；需要交付或协作时推送当前分支到受控远程。
8. 发布版本时更新 `VERSION`，将“未发布”内容移入带日期的版本章节，创建带说明标签并推送分支与标签。

若仓库没有远程或推送失败，必须在交付说明中明确写出“仅有本地提交，远程备份未完成”，不得表述为已经完成备份。

## 4. 提交信息

格式：`类型(范围): 摘要`

常用类型：

- `docs`：说明书、规范、日志或其他文档。
- `feat`：新增玩家可见功能或系统能力。
- `fix`：修复错误或缺陷。
- `data`：Schema、配置或内容数据。
- `test`：测试、夹具或验收用例。
- `build`：构建、依赖或发布流程。
- `chore`：不改变产品行为的维护工作。

示例：`docs(gdd): 补充存档迁移验收标准`

## 5. 发布检查

- 文档内版本、文件名、`VERSION` 与标签一致。
- `CHANGELOG.md` 有发布日期和完整摘要。
- DOCX、PDF及其目录/页码对应同一内容。
- 需求、Schema、测试用例和受控子文档已同步。
- 工作区干净，发布标签指向正确提交。
- 发布分支与带说明标签已推送受控远程。
- 已确认可以从远程检出发布提交；Git LFS 或外部资产中的必需文件也可恢复。

## 6. 分支与合并

- 小型、低风险文档维护可在当前批准的工作分支完成；功能开发使用说明性短生命周期分支。
- 分支建议格式：`类型/简短主题`，例如 `feat/combat-loop`、`fix/save-migration`、`docs/dev-standard`。
- 合并前必须通过适用测试和审查，不得以合并解决未确认的冲突。
- 提交应可独立理解和回退；格式化或批量机械修改应与行为变更分开。
- 合并后删除已完成的短期远程分支前，确认提交已进入受保护默认分支。

## 7. 远程备份与恢复演练

- 仓库至少配置一个访问受控、具备适当权限与多因素认证的远程；推荐将 `origin` 作为主要协作与备份远程。
- 默认分支、当前发布分支和全部发布标签必须推送远程；高价值里程碑可额外保留只读镜像。
- Git 托管仓库不自动覆盖数据库、云配置、密钥和外部大文件，这些内容需要各自的安全备份方案并记录恢复路径。
- 建议每月及重大发布前从空目录执行恢复演练：克隆仓库、检出最近标签、获取大文件、安装锁定依赖并完成基础构建/文档打开检查。
- 恢复演练应记录日期、执行人、结果、缺失项和整改期限。
- 远程地址、权限或托管平台变更属于受控操作，变更后必须验证拉取与推送，并保留旧远程直至迁移验收完成。

## 8. 禁止与例外

- 不得使用 `git reset --hard`、强制检出、清理未跟踪文件等破坏性操作处理来源不明的工作区修改。
- 不得使用 `--force` 推送共享分支；经批准确需更新个人分支时也优先使用 `--force-with-lease`。
- 不得删除发布标签或复用已发布版本号；更正发布应产生新修订版本。
- 例外必须记录批准来源、风险、保护措施、恢复步骤和有效期限。
