# 修改日志

本日志记录项目内已批准的文档、设计、数据、代码与发布物变更。格式参考 Keep a Changelog；版本号采用 `主版本.次版本.修订号`。

## [未发布]

### 部署

- 将线上部署从 Cloudflare Workers 完整迁移至 EdgeOne Makers（国内直连，region=china）。因 Vinext RSC 默认产出 SSR（无 index.html），新增 `next.config.mjs` 设 `output:"export"` 与 `images.unoptimized:true`，使 `vinext build` 预渲染为静态站点（`dist/client/index.html` 等），经 edgeone-pages MCP `deploy_folder`（projectType=static）上线；已验证首页、JS chunk 与背景图均返回 200。Cloudflare Worker（shanhai-yiwen-system-preview.302628809.workers.dev）保留为兜底，可按需停用。
- 重新部署第一卷《黑雨》v4.1.0（464 节点）：内容包更新后重新构建静态导出并部署 EdgeOne（V1.4 对齐的 5 页导航、撤回、branchClass、高亮跳转、稀有度标签、幕名修正等代码改动同步上线）。部署前打基线 tag `deploy-v4.1.0-20260814`（指向 commit `89451b3`），剧情内容包完整快照另冻结于 `剧情/.../备份/2026-08-14_v4.1.0/`（zip，含 10 个 JSON + 正文 + 资料）。已验证线上 HTTP 200、`contentVersion:"4.1.0"`。
- 信息栏（情境安全区）从底部移至标题栏正下方：`StorySafeArea` 的 DOM 位置从 `desktop-grid` 之后移到 `topbar` 之后；`.story-safe-area` 由 `position:fixed; bottom:0` 改为 `top:64px`（移动端 `top:calc(64px + env(safe-area-inset-top))`），`border-bottom:0` 改 `border-top:0`。配套调整：`.mythic-shell .reader` 顶部 padding 42px→106px、`.side-rail` 顶部 padding 30px→94px（避让信息栏）、移动端 `.mobile-status-strip` 加 `margin-top:72px`、移动端 `.reader` 底部 padding `calc(196px+…)`→`calc(104px+…)`（底部信息栏移除后不再需要大留白）。滚动定位（视口 2/3 / reducedMotion 1/3）不变。
- 导航重构与版面优化（三项）：① 底部导航由「志异/故交/行游/舆图/命录」改为「志异/命录/行游(居中)/行囊/舆图」——命录提前到第 2 位，行囊独立为第 4 大功能页（`PanelId` 移除 `people`、新增 `inventory`，`hlPanel` 同步：人物→命录、物件/线索→行囊）；命录页子标签由「行囊/未竟之事」改为「故交/命轨」（故交渲染 PeoplePanel、命轨渲染 QuestLog）。② 功能页标题去重：`reader-heading`（eyebrow/h1/brush-rule/退出按钮）仅在 story 页渲染，非剧情页由面板内部标题唯一呈现；MapPanel、FatePanel 补统一 `archive-toolbar` 标题（「舆图」「命录」）。③ 顶部大框：`.mythic-shell .topbar` 去掉 `border-bottom`，与信息栏 `.story-safe-area` 连续合并为统一深色大框。
- 信息栏仅「行游」剧情界面可见：`StorySafeArea` 渲染条件加 `panel === "story"`；`main` 加 `data-panel={panel}`，用 `[data-panel="story"]` 选择器让避让 padding（`.reader` 顶部 106px、`.side-rail` 顶部 94px、移动端 `.mobile-status-strip` margin-top 72px）仅在剧情页生效，非剧情页恢复默认（reader 42px / side-rail 30px / 状态条 0）。信息栏成为剧情界面专属模块。
- 剧情界面排版与交互规范（5 项）：① 选项栏内部顺序固定为「序号 + 类型 + 选项内容 + 箭头」——按钮 DOM 改为 `span(序号)/em.choice-type(类型)/b.choice-label(内容)/i(箭头)`，grid 改 `42px auto 1fr 26px`，类型渲染为小标签（说话/行动/来源）。② 系统提示两行排版——`story-system` 拆为 `b.story-system-title`（标题，按首个冒号提取、缺省「异兆记录」）+ `p.story-system-body`（内容），打字机仅作用于正文、标题固定。③ 取消文字特效时定位最新内容顶部至界面 1/3 高度——自动滚动 `fraction` 由 `reducedMotion ? 1/3 : 2/3` 改为 `shouldType ? 2/3 : 1/3`，且无特效时定位 `latestNodeRef`（顶部）而非底部。④ 逐字时上翻查看历史不冲突（沿用 IntersectionObserver `awayFromLatest` 暂停自动滚动 + tw-stack 双缓冲稳定高度）。⑤ 剧情范围内双击立即取消特效并保持位置——`story-transcript` 加 `onDoubleClick={revealAll}`，`revealAll` 一次性显示全部剩余内容，用 `skipNextScroll` ref 跳过 reveal 触发的自动滚动，防止阅读位置跳动。

### 新增

- 山海志改为与行囊一致的单卡片标签结构，移除下拉分类；按第一章叙事重要性重排为异象、人物、地理、异兽、器物、材料、文书，其中器物合并器具，并改为无横向滚动的自动换行标签。
- 山海志与行囊进一步合并“材料”和“器物”标签；新增首次进入界面，引导新用户先设置角色信息，并补充可生效的游戏设置项：自动保存、触感反馈、阅读、音量、纹理与动效。
- 山海志详情按“初见、传闻、行证、推论、余响”展示已得与待求证的见闻层，并保留来源与异说提示；行囊新增七个在身装备位，分离装备、随身物与物品认知／剧情用途信息。
- 完成第一卷《黑雨》v3.0.0 全量扩写：内容包扩展为 365 个可达剧情节点，生成全节点小说母稿 110,295 汉字；新增“黑雨七日记”“六件普通事”“未送达回执”“雨后初课”四组可回访剧情，分别将灾日记忆、材料互助、照料/拒绝边界、失散回执与公共规则演练接入既有剧情状态和终局前入口。
- 在角色栏、角色档案与竖屏状态条三处展示六维属性（体魄、身法、灵识、心志、机巧、言契），并带旧存档兜底；修复竖屏状态条在默认竖屏宽度下残缺/不可见的问题。
- 支持多角色存档与主页角色选择：每名角色独立保存出身、六维、位置与进度；主页存在角色卡时默认进入角色选择而非重新创建，可进入、删除或新建角色；旧版单存档在载入时自动迁移为角色进度，不丢已有进度。
- 剧情文本实体高亮：人物、道具／材料、地点三类名称以不同颜色加粗显示（朱砂橙／金／苔绿，暗色主题下用更亮色保证可读性），覆盖剧情正文、对话、系统记录与详情卡片。
- 补充开场角色交代：卷首明确玩家角色身份与所处情境（按出身插值「你是杳湾的〈出身〉，此刻正〈情境〉」），使「我」的存在不再悬空。
- 新增第一幕《雨至》与第二幕《三影》分支剧情系统（`app/game/branching/`）：每决策节点可选项严格 ≤3 且显式编号（`no`），避免单一结局导向；第一幕三条路线（记历棚/渡口/粮仓）收束于幕终「承诺角色」，据角色分流到第二幕三个入口；第二幕于北滩浮尸处分三后果轴（羿箭 / 迟日 / 含晦），玩家选择引发不同走向；第一幕 flag 经 `visibleWhen` 跨幕影响第二幕可见分支与文本（如渡工线偷看货→第二幕渡口专属选项、取昼盐→独行线取盐选项）。引擎含 `validate()` 强制 ≤3 选项与 `next` 可达，并提供 `scripts/verify-branching.ts` 校验（连通性、三路线贯通、跨幕交叉影响、自动备份与恢复均通过）；剧情数据生成/更新时经平台适配层自动写带时间戳副本，支持 `list`/`restore`（`StoryDataBackup`）。说明见 `docs/branching-acts12.md`。
- 第一卷《黑雨》内容包升级 `v3.0.0 → v4.0.0`（全卷选项规范化）：第一、二幕 68 个旧节点替换为分支剧情 19 节点（含三卷终：羿箭轴·箭痕未冷 / 迟日轴·迟日有名 / 含晦轴·盐封启闭，分别桥接第三幕入口）；第三四五幕 42 个超限节点按「无损拆分」扩展为 147 个分组中间节点（每节点选项 ≤3，不删除任何已批准分支）；全部节点选项补 `no` 编号。节点数 365 → 463（全部入口可达），正文母稿重新生成为 99,934 汉字；替换前完整快照冻结于 `备份/2026-08-14_v3.0.0/`。生成脚本：`tmp/apply_branch_v4_step1.py`、`tmp/apply_branch_v4_step2.py`。
- UI 与内容优化一批（R1/R4/R5/R7/R8/R9）：设置面板新增「文字出现速度」滑块（`Settings.textSpeed`，默认 1x、范围 0.5~3，持久化，打字机延迟 30/280ms 按比例缩放）；开启「减弱界面动效」时最新剧情定位由视口 2/3 改为上部 1/3；信息栏「所在」改为「大地点·场景」两级（`sceneLabels` 将节点 `presentation` 映射为中文场景，如「杳湾 · 副本深处」）；「更多」菜单事件区块「进行中的事件」改名「任务」并新增「已完成事件」标签（据 `activeQuests`/`completedQuests` 区分，标题支持点击收起/展开简介，默认展开）；界面全面汉化（`NEW GAME`/`CONTINUE`/`SETTINGS`/`LOCAL FIRST`/`ABOUT` 等 eyebrow 与 `ACT*` 章名 `chapterLabels` 映射为中文，`内容 v` 改「内容版本」，保留游戏英文副标题为品牌名）。
- 按 V1.4 说明书对齐信息架构与交互（§2.3⑤/§13）：底部导航由 6 面板重构为 5 页「志异(codex)／故交(people)／行游(story,居中放大)／舆图(map)／命录(fate)」，拆掉「更多」菜单——存档与系统说明并入右上角「设置」、事件与最近记录并入「命录」（命录页含「行囊／未竟之事」两标签）；顶栏右上角只保留「设置」。新增「连续撤回 2 次选择」（`undoStack` 快照栈，选择前压栈、最多 2 个，撤回回退到上一选择点且不重复写入世界状态，选项区上方显示「↩ 撤回上一步」按钮）。选项分级落地 `branchClass` 代码口径（`BranchClass`/`branchClassLimit`：sediment≤3/reflow≤4/fork≤6，缺省 sediment，`validateBranchClass` 供数据校验）。正文高亮由 3 类扩为 5 类（人物/物件/地点/线索/异象，`entityNames` 增 clue/anomaly），高亮词可点击跳转对应功能页（人物→故交、地点/异象→志异、物件/线索→命录）。行囊/详情展示物品稀有度分级标签（凡物/精良/珍异/任务/神话，§5.3）。第五幕《十日之兆》→《赤日之兆》、第三幕《逆流之河》→《倒河》（§2.3①/A1）。

### 修复

- 修复 iOS Safari 添加到主屏幕（PWA standalone）顶部剧情内容透出与状态栏重叠：viewport meta 追加 `viewport-fit=cover`，并补 `apple-mobile-web-app-capable=yes` / `mobile-web-app-capable=yes` / `apple-mobile-web-app-status-bar-style=black-translucent` / `apple-touch-icon` / `theme-color=#17130f` 等 PWA meta，使 `env(safe-area-inset-top)` 在 standalone 模式下正确返回状态栏高度（约 59px），topbar 的 `height:calc(64px + env(...))` + `padding-top:env(...)` 覆盖状态栏区域；`html, body` 背景改为 `#17130f`（与 topbar 同色）兜底遮盖。新增 `scripts/inject-pwa-meta.mjs` 构建后注入脚本（Next.js `Viewport` 静态导出丢失 `viewportFit` 字段，用 post-build 兜底）；`package.json` 的 `build` 脚本改为 `vinext build && node scripts/inject-pwa-meta.mjs`，`layout.tsx` 用 `metadata.other` 输出 apple meta（确保未来构建即包含）。`layout.tsx` 的 `viewport.themeColor` 同步 `#17130f`，icons 增加 apple 图标。

- 顶部与底部安全区改为不透明、中间缓冲区加高、移除山海志/行囊退出按钮：`.mythic-shell .topbar` 背景由 `rgba(23,19,15,.98)` 改为纯色 `#17130f`（base `.topbar` 同步 `#141a15`），手机灵动岛/刘海区域（`env(safe-area-inset-top)` 覆盖区）滚动时不再透出剧情内容；底部 `.story-safe-area` 背景由 `rgba(...,.92)` 改为纯色 `#1f1b17`（暗色主题 `#17130f`），完全遮盖下方内容；`.story-buffer` 空白缓冲区高度由 28px 增至 140px（移动端 22px→110px，即 5 倍）；`reader-heading` 的「退出到主页」按钮仅当面板不是山海志（codex）/行囊（inventory）时渲染，取消这两处的退出按钮及其功能。

- 修复对话与选项卡片出现时的闪烁：移除打字中消息的额外底部内边距（`.story-message.is-typing` 的 `padding-bottom:10px`，是旁白/系统消息打字完成瞬间 10px 高度突变的根源）；为对话消息与选项按钮新增一次性入场动画 `@keyframes story-enter`（淡入 + 轻微上移，对话消息 `fill-mode:both`、选项按钮 `fill-mode:backwards` 以免钉住 hover 的 transform），动画仅在元素首次挂载时播放、逐字更新（children 变化但 key 不变）不重复触发；选项按钮按索引错峰（内联 `--i` 变量乘 55ms 延迟）依次淡入，使从「……」占位切换到选项卡片时平滑自然。不影响既有逐字展开、自动跟随滚动与其他动画表现。

- 分支剧情系统类型检查修复：`types.ts` 对 `src/content/contracts` 的相对导入路径由错误两级（`../../src/content/contracts`，指向不存在的 `app/src/content/contracts`）改正为三级（`../../../src/content/contracts`）；并补齐 `types.ts` 对 `Predicate/Effect/StoryBlock` 的「本地导入 + 再导出」与 `engine.ts` 对 `BranchState` 等类型再导出，使 `engine.ts`/`scripts/verify-branching.ts` 的类型引用可在 `tsc` 下解析；`tsconfig.json` 开启 `allowImportingTsExtensions`（已 `noEmit`，仅影响类型检查）以放行验证脚本的 `.ts` 扩展名导入。修复后 `tsc --noEmit` 全量错误回落至基线 19（分支系统零新增错误），`scripts/verify-branching.ts` 五组校验仍全绿。

### 文档

- 支线角色改名（内容包 4.3.1→4.3.2）：「季璃」改名「季缨」（jì yīng），与北方使者「季狸」(jì lí) 读音彻底区分，消除同音混淆。内容包 18 处替换（角色卡 name/aliases 改「红丝/季槐之姐」+ 正文 17 处），「季狸」84 处原样保留未误伤；build 全绿重生成正文（季缨 19 处、季璃残留 0）。新名呼应角色意象：缨=丝织带穗，直指她手中井边红丝，有系留牵挂之义，贴合守井等弟的隐忍。备份于 `备份/2026-08-15_v4.3.2/`。

- 第一卷全面校对修正（内容包 4.3.0→4.3.1，结合说明书 V1.4）：① 12 处 ASCII 单引号改中文引号（消除中英文标点混用）；② N014 材料显形提示英文内部 ID「IT-V01-MAT-EMBERLEAF」改中文「扶桑烬」（去除用户可见英文）；③ N355「剑婆」错别字改「缄婆」；④ N344 两处季璃对话标签误标「季狸」改「季璃」——经核查季璃为独立支线角色（弟弟季槐被推入盐井，正文出现 17 次），与北方使者季狸（NPC-JILI）同音不同人，原 speaker 误用 NPC-JILI；⑤ 新增季璃角色卡 NPC-JILI2；⑥ 角色数 11→12。母文件版（内容包+正文+目录说明）与扩写版分别备份于 `备份/2026-08-15_v4.3.1/`。

- `黑雨_文学扩写版.md` 同步至 v4.3.0：幕结构由旧六幕制（尸体与证词/桑林与盐井/无日之夜/让谁先知道）重排为 v4.3.0 五幕制（雨至/三影/倒河/无日之夜/赤日之兆），场景归属按内容包 chapter 校正（场景二「第一夜的议事」归入第一幕 A1-MID、场景三「北滩有尸」归入第二幕 A2-SHORE 等）；8 处失效接入节点更新为当前有效 id（第一二幕改用 `A1-*`/`A2-*` 分支节点，第三四五幕保留 `V01-C01-N*` 并标注幕归属）；场景五补 v4.3.0 新增昼盐命令签发者伏笔（N315 撕角文书呼应）；卷首标注版本同步说明。8,755 汉字（原 8,475 增加约 280 字）。

- 新增第一卷全量审查报告 `docs/black-rain-v1-review.md`：程序化图遍历（选项归一化比对 / Tarjan 环检测 / 从终局 N023 反向可达分析 / 入边与章节归属核查）+ 逐幕人工叙事核对。结论：主线骨架（一二幕分支 + 三-五幕总分 + 六卷终）逻辑自洽；**P0 严重问题 13 项**——N296 跨卷回环（第五幕选项误指第二幕卷终 A2-END-SALT，剧情时间线倒退）、10 个回访死区枢纽 + 2 个中间枢纽（N286/N281/N291/N306/N311/N315/N320/N325/N335/N340 及 N053-G11-G21/G31，进入后无出口回主线，玩家被困无法到达卷终）、build 校验缺反向可达检查；**P1 重要 2 项**——N227 三选项文案归一化后完全重复（仅（一）（二）（三）差异，命中用户排查第 b 类）、22 个回访链节点 chapter 标签错乱（七日记跨标 ACT1-5 等）；**P2 建议 5 项**——第三幕节奏偏平、金乌/第十日/昼盐命令签发者/赤金箭嫁祸者 4 项第二卷伏笔未埋、卷终单选项无再决策空间、HUB 标题机器痕迹、N053/N022 标题近似。每项均附修改动作与可复现脚本口径（修复后反向可达应覆盖 ≥316 节点、死区归零）。

- 第一卷剧情完整性/连贯性修复（内容包 4.2.0→4.3.0，依据 `docs/black-rain-v1-review.md`）：① **消除全部死区**——10 个回访死区枢纽（第三幕归还桌 N286、第五幕轮班表 N281/记忆修复台 N291/书信桌 N306/孩子短会 N311/旧档案间 N315/演练复盘 N320/第一次听证 N325/门槛回访 N335/黑雨七日记 N340）的 42 个子场景各补 1 个「返回主线」出口选项（→N214 或 →N053），玩家不再被困在区内循环；② **修复 N296 跨卷回环**——第五幕「三段红线」的「回归还者号」选项由误指第二幕卷终 A2-END-SALT 改为第三幕沉船线 N061（可达主线），消除时间线倒退；③ **N227 出身旧物三选项差异化**——由「检视旧物（一/二/三）」改为按木架分层内容命名（猎户骨箭/山民扁石/巫祝祭灰、商旅钱袋/九黎骨文/祭司日名、流亡贵胄断印/无名白牌）；④ **22 个回访链节点 chapter 标签统一为第五幕**——黑雨七日记（N341-347）、六件普通事（N349-354）、未送达回执（N357-360）、雨后初课（N363-365）不再跨标 ACT1-5，正文分幕正确；⑤ **补人物出场铺垫**——A2-SHORE 北滩有尸补 narration（隗青为最先发现者、季狸北方马车、砚童供粥伏笔）、A1-GRANARY 青芽对话提及阿禾（牧羊女身份与雨天见识）、N013 河床补黎苇（盐泽流民首领）铺垫；⑥ **补第二卷伏笔**——N315 旧档案间（昼盐命令签发者撕角文书）、N021 羿线（赤金箭刻痕非弓弦所磨、首现沾血可疑=嫁祸者）、N028 余响（云后金乌振翅、七日之后不可数=第十日）。校验：真死区 0（反向可达仅余 51 个结局链正常单向流）、build 全绿（374 节点全可达）、正文重生成 93,278 汉字。修复前快照冻结于 `备份/2026-08-15_v4.3.0/`。

### 调整

- 剧情内容上下文与选项体验优化（内容包 4.1.0→4.2.0）：① 删除 16 处剧透/导向 system 块（结局名预告「进入『把未来交给射手』结局」、内部变量 `ending.*`、第二卷接口、卷末预告），中性改写 93 处「副本完成/已写入/将影响后续/解锁」导向句为纯完成记录，保留必要玩法规则提示；② A2-SHORE 分叉提示改为不泄露分支方向的中性引导，A2-END-* 卷终变量提示删除；③ v4.0.0 无损拆分产生的 144 个「继续：xxx（第N部分）」机器文案选项处理——30 个 4 选项节点内联恢复为单节点（标 `branchClass=reflow`），54 个保留 HUB 入口改手写语义化引导文案，54 个 HUB 占位 blocks（「你继续处理xxx的分支」）改为有语境的场景过渡叙述，删除 90 个孤儿 HUB 节点，节点 464→374；④ 选项剧透词修正（N015「把真相告诉砚伯」→「把砚童喂尸的事告诉砚伯」）。UI：选项框去掉「待决定 · 」前缀，类型提示（说话/行动/来源）改为独立徽章（`.choice-type`），对话选项不再用朱砂边框色改变图块颜色（`speech-choice` 颜色中性化，类型由徽章表达），避免类型影响选择判断。校验：build 全绿（374 节点全可达/6 结局）、分支校验全绿、分支系统 tsc 0 新增错误。

- 分支剧情结构由「总分总」调整为「总分」：第二幕三条后果轴（羿箭 / 迟日 / 含晦）不再汇流到同一个 `A2-END`（act-end）→ `A2-DONE`（卷终）单一结尾，改为各自落到三个互不相同的卷终节点 `A2-END-ARROW`(箭痕未冷) / `A2-END-SHADOWS`(迟日有名) / `A2-END-SALT`(盐封启闭)（`kind:"volume-end"`，置 `completed=true`）。玩家在第一、二幕的分支选择最终导向三种不同结局，不再收口成一种「待续」。节点数 18→19，`engine.ts` 逻辑无需改动（volume-end 通用处理），`scripts/verify-branching.ts` 第 3 组增强为「三种角色 × 三条后果轴」九条路径均抵达各自卷终且世界变量正确；`tsc` 全量错误维持基线 19。说明见 `docs/branching-acts12.md`。

### 调整

- 对齐《游戏设计说明书暨开发任务书 V1.4》七卷机制统一口径（§2.3/§3.1/§5.3/§7.3/§16）：① 第五幕显示名《十日之兆》→《赤日之兆》（枚举 `ACT5_TEN_SUN_OMEN` 不动，build 脚本/GameShell 章名/正文母稿三处同步；第三幕章名同步修正为《倒河》）；② 四分抉择 N023「黑雨停了」从 v4.0.0 的三层拆分恢复为 `fork` 命运节点单节点——6 个不可逆选项（交羿/东行/折箭/北方交易 + 隐藏·封井永守 + 隐藏·三影者自由），删除 3 个中间 HUB 节点，节点数 463→460；③ 新增 2 个隐藏卷末结局 N265「井口不是公堂（封井永守）」与 N267「三影者自己决定（自由）」，卷末结局 4→6（4 主 + 2 隐藏，tags 含 `hidden_route`，`visibleWhen` 前置条件解锁），manifest mainEndings 4→6；④ StoryNode 增 `branchClass`（sediment≤3/reflow≤4/fork≤6 且全部不可逆），内容包 13 节点显式标注（1 fork + 12 reflow，其余缺省 sediment），`build_black_rain.py` validate 与分支引擎 `engine.validate` 均新增分级校验，`contracts.ts`/`branching/types.ts` 类型同步；⑤ 出身三层：`player-origins.json` 新增 `natures`（6 种，含多疑/执拗）与 `flaws`（6 种，含体弱/易招异兽）数据段，`blackRainContent.ts`/`gameData.ts` 改为内容包优先读取（旧内容包缺字段回退内置），`types.ts` 的 NatureId/FlawId 扩展；⑥ 新增大型事件副本任务 `Q-V01-EVENT-NORTHBEACH`「第七日·北滩长桌」（四阶段：对峙→四方陈述→谈判抉择→终局结算，7 名 NPC 参与，世界状态结算，不设必须击杀目标），补齐 V1.4 §7.3「3 小型 + 1 大型事件副本」的 Demo 配置，quests 31→32。内容包 contentVersion 3.0.0→4.1.0。branchClass 分级校验通过、分支校验脚本全绿、`tsc` 分支系统 0 新增错误。

### 文档

- 新增分支剧情详版思维导图 `docs/branching-mindmap.html`：在保留「总分」结构连线的基础上，把每个节点的**人物、剧情原文、三选项与后果变量**，以及两幕间的**背景设定与发展脉络**（黑雨世界观、幕间交叉影响逻辑、第二幕三身份立场、北滩三影者/含晦设定、三后果轴的三种真相姿态）直接标注在对应区域，无需另查文档即可读懂；含可点击跳转的全局结构总览与可折叠节点卡。所有文案与 `app/game/branching/acts.ts` 一致，脉络框为对分散 narration 设定的集中标注，未新增剧情内容。
- 新增七卷总剧情思维导图 `docs/seven-volumes-mindmap.html`：覆盖全部七卷情节的自包含导图。卷一《黑雨》以已落地完整内容详展开（五幕结构、三个中型副本、十二条人物线、四条结局与三条后果轴、约十项卷一→卷二跨卷状态绑定）；卷二至卷七依 V1.3 总纲以「规划待落地」节点呈现，直接于节点旁标注主题、主角阶段、关键玩法、关键 NPC 与跨卷输入/输出，并附「七卷情绪重心与玩法升级」附表。导图采用卷主干 limbs + 总纲中枢 + 可折叠节点卡，读者无需额外查阅即可理解整体脉络与总分结构；数据来源：V1.3 说明书、第一卷小说母稿、10小时扩展蓝图、内容包 characters/world-state JSON。
- 新增分支决策树 XMind 文件 `docs/black-rain-decision-tree.xmind`：以标准 XMind Zen 格式（ZIP 内含 content.json）可视化第一幕《雨至》+ 第二幕《三影》的全部选项→结局走向。树状结构含「起点 → 三初始去向 → 三落脚处 →〔收束〕村中夜议 → 井/守/北滩 →〔幕终〕承诺身份 → 三第二幕入口 →〔收束〕北滩有尸 → 三后果轴 → 三卷终」，每个分支节点以「选项N」标签 + 注释标注其文案、跳转目标与后果标记；并以「📊 统计汇总」分支与根节点汇总展示：总走向路径 5265 条、卷终结局 3 个（羿箭轴/迟日轴/含晦轴，互不汇流）、结局子态 9 种（每轴 3 世界变量分支）、每结局汇聚 1755 条。统计由脚本精确枚举所有可达决策序列得出（含 2 个有条件出现选项：A2-FERRY-c3 需先『偷看舱中物』、A2-SOLO-c1 需先『取过昼盐/知含晦』），数据严格来自 `app/game/branching/acts.ts`；生成脚本保留于 `tmp/gen_xmind.py`。
- 新增第三至第五幕互动剧情更新框架 `docs/branching-acts345-design.md`：延续第一、二幕分支规格（每节点 ≤3 编号选项、`visibleWhen`、`effects`、总分结构），系统设计第三幕《倒河》（三线：河床/东桑/村中，桥接 A2-END-* 三卷终）、第四幕《无日之夜》（井下含晦 + 羿/黎苇/季狸阵营二分叉）、第五幕《十日之兆》（A5-FINAL 最终抉择两步裂出**6 个卷终结局**：交羿/东行/折箭/北方交易 + 隐藏·封井永守/隐藏·三影者自由）。全卷 3 大主干走向（求真·调查线 / 守护·人际线 / 借势·阵营线）从第一二幕三后果轴持续延伸、互不回归；每幕埋设第二卷《十日》伏笔（隐藏角色逢蒙/北方主印者/羲和帝俊/金乌、未解之谜六项、暗示事件），与 `world-state.json` 的 V02 echoBindings 对齐。叙事一致性核验：黑雨七日时间线、11 NPC 性格、副本/人物线沿用受控基线，文风延续卷一（参考百年孤独的循环宿命、沧月《镜》的苍凉史诗、江南的群像热血）。
- 新增七卷剧情时间轴 XMind 文件 `docs/seven-volumes-timeline.xmind`：以**时间轴形式按卷推进**（一卷接一卷、非并列；根节点启用 `org.xmind.ui.timeline.horizontal` 时间轴结构，卷节点以 ①→⑦ 编号）。时间轴上依次为「📊 统计汇总 → ① 卷一《黑雨》→ ② 卷二《十日》→ ③ 卷三《逐鹿》→ ④ 卷四《天柱：不周山》→ ⑤ 卷五《九州洪流》→ ⑥ 卷六《昆仑：不死之国》→ ⑦ 卷七《天地有名》→ 🏁 七卷终局」。每卷节点直接标注剧情简介（副题/主角阶段/核心命题/关键玩法/关键 NPC）、剧情推动节点与跨卷输入/输出；卷一为已落地详展开：五幕推动节点 + 第一、二幕完整分支决策树（每节点「选项N」+ 注释标注文案/跳转/后果标记）＋**分散/汇总点显式标注**（分散：起点、幕终身份、三后果轴；汇总：村中夜议、北滩有尸；承接：三轴跨卷传递），并附三后果轴卷终（各汇聚 1755 条）、三中型副本、跨卷输出变量。统计汇总分支显示：总走向路径 5265 条、已实现卷终结局 3 个、结局子态 9 种、每结局汇聚 1755 条、七卷宏观结局规划 8—12 个，并附「卷推进路线」链式节点与计算口径（卷二至七未实现分支，不参与枚举）。生成脚本保留于 `tmp/gen_xmind_timeline.py`。
- 新增《山海异闻录》说明书 V1.4（Markdown，`说明书/山海异闻录_天地未定_游戏设计说明书暨开发任务书_V1.4.md`）：以 V1.3 docx 全文（脚本提取 688 行、63KB）为基底，并入七卷机制总纲评审结论，新增 §2.3「七卷机制统一口径」（层级卷→幕→节点废弃章／选项分级 branchClass／七二一+阶段汇流／结局三级口径／页面命名／卷间继承），并在 §12.2/§13.1/§16.2/附录A 标注修订、附录B/D 登记决策与修订、新增附录E「V1.4 变更单登记」（A1 第五幕改名《赤日之兆》／A2 branchClass Schema 扩展／A3 关系轴登记／A4 新卷 ID 规范）。V1.3 的 docx+pdf 已备份至 `说明书/备份/2026-08-14_v1.3/`。提取/转换脚本：`tmp/extract_docx.py`、`tmp/convert_to_md.py`。
- 按《说明书修改意见》修订说明书 V1.4（`说明书/...V1.4.md`）与《七卷机制总纲》（`docs/seven-volumes-mechanism.md`）：① Demo 平台改为线上预览、微信小游戏保留平台抽象边界（维持 Web 优先）；② 功能页面统一为五命名「志异/故交/行游/舆图/命录」（行游居中为最大主入口、设置右上、主角属性左上、家宅 Won't、成就入命录、存档入设置）；③ 新增 §5.3 物品分级（rarity：common/uncommon/rare/quest/mythic）；④ §6 战斗简化为「以剧情推动的特殊剧情节点」（保留知识型多解、弱化数值对抗）；⑤ §13 UI 改为暗色墨底主题（朱砂+金+苔绿）、正文四类、实体高亮点击跳转、选项卡片样式、连续撤回 2 次、情境信息栏；同步更新 §2.3⑤ 与子文档 §5/§6.3/D3。

### 修改

- 参考 MUD 游戏界面改造剧情区与沉浸感（第一批）：章名行新增状态徽章（异兆缠身／任务中／暂无重伤三色）与「退出到主页」快捷入口；关键选项（带来源）改用朱砂描边 + 稍大尺寸形成视觉层级；暗色主题叠加极淡噪声纹理、阅读区顶部加暗角、章名 eyebrow 右侧加朱砂小印，强化氛围与质感。
- 「情境 · 安全区」从剧情面板内部移出，改为与剧情内容区**并列**的独立区域（`StorySafeArea` 组件，与 `StoryPanel` 兄弟节点），语义上安全区不再属于剧情正文范围。

- 参考 MUD 游戏界面改造（第三批）：舆图当前位置加「此间」金色高亮徽章；主页角色卡新增六维属性进度条（属性名 + 进度条 + 数值）；顶部／底部导航当前面板改用金色高亮下划线，强化信息层级与当前定位。

- 游戏区底部预留安全区：底部导航、意图栏、提示条与正文底部内边距适配 `env(safe-area-inset-bottom)`，避免内容被固定层遮挡，便于阅读与手势操作；待做决定的选项框改为方角矩形并使用深红底色。

- 优化剧情区阅读体验：最新内容自动滚动到底部、逐字展开期间不提前显示下一次选择、旁白与系统记录统一显示在对话气泡之外、对话气泡格式统一，层级更清晰。

- 收紧对话气泡内边距并增大人名与正文字号（人名加粗、桌面 15px／移动 14px），文字与人名清晰易读；逐字展开改为「隐藏完整文本占位 + 可见文本逐字覆盖」的双缓冲渲染，版式稳定不再随换行跳动；自动跟随滚动改用瞬时定位，逐字过程流畅无顿挫；移动端正文底部内边距加大，彻底清开底部导航与意图栏。

- 进一步压缩剧情段落间距（暗色主题段落间距 30px→14px、分隔线 27px→14px，移动端同步收紧）使排布更密集自然；对话正文字号再增大（桌面 16px、移动 15px，人名 14px／12px），并在逐字展开的文本框下方预留空隙，避免内容紧贴边缘。

- 剧情内容自动跟随改为定位到视口约 2/3 高度处（底部留 1/3 空白），避免最新文字贴靠底部边缘；移除与「情境 · 安全区」重叠的消息提示便签条（toast 元素及其 CSS 全部删除），提示信息统一由安全区展示。
- 剧情区与「情境 · 安全区」之间新增空白缓冲区 `.story-buffer`：无任何内容与交互元素，仅以顶部虚线/分隔色与相邻区域形成视觉边界；移除原「剧情正在展开 / 显示全文 / 回到最新」控制条。同时增加信息栏（`.story-safe-area`）的垂直内边距与最小高度，使所在、时日、提醒等行不再拥挤；同步加大移动端正文底部内边距，避免内容被增高后的信息栏遮挡。

- 新增独立的游戏开始页面（landing page）：玩家首次进入显示全屏深色居中布局——标题「山海異聞錄」+ 副标题「天地未定 · THE UNWRITTEN SHANHAI」+ 简短世界观描述 + 中央金边框按钮「入山海」，点击后进入角色选择/创建页；首次进入状态用 localStorage（`shanhai.landingSeen`）持久化，避免每次刷新都重新展示。三步流程：开始页面 → 角色选择/新建 → 进入游戏。

- 「情境 · 安全区」从剧情面板（StoryPanel）内移出，改为与游戏剧情区并列的独立范围区（GameShell 顶层 fixed 底部，所有面板均显示），不再嵌套在剧情容器内部。

- 修复剧情回访节点历史重复显示的严重 bug：回访已访问节点（如「让砚童去找阿禾核对石窑来客」回到羊有四片夜）时，改为截断 storyHistory 并重新呈现该节点内容，避免旧节点历史被错误重复显示。

- 在剧情区底部新增「情境 · 安全区」，固定显示当前所在、时日（第几日 / 时段）与最新提醒，与正文有明确视觉边界（金色边框 + 顶角标签 + 虚线分隔），便于随时核对情境且不遮挡剧情。

- 「情境 · 安全区」改为真正 fixed 固定在视口底部（桌面端避开左侧角色栏 left:270px，移动端位于底部导航之上并适配安全区），剧情滚动时始终可见；删除「写下意图」输入条形栏（`.command-dock` UI 整段移除），剧情推进改为以选项为唯一入口。

- 详情卡片在桌面与手机浏览器中均居中显示并可在卡内滚动，不再作为移动端底部抽屉；调整山海志与行囊的界面层级，使其分别服务于认知复盘和行旅准备。
- 优化竖屏（750×1334）移动端布局：简化开始界面、压缩角色创建步骤高度使其一屏放下、设置界面新增「退出到主界面」按钮。
- 将第一卷内容包及其所有数据文件统一升级至 `v3.0.0`；同步章节目录说明、接入说明、十小时蓝图和扩写计划的实际节点与文本统计。完整探索时长仍待新档试玩验证，不以文本规模替代实测。

- 新增《黑雨》雨棚长对话调查链：宿沙、青芽、隗青、砚伯与缄婆各自说出恐惧、边界和不愿转嫁给他人的代价，使人物冲突由功能性任务推进到可在终局追问的个人承诺。
- 补齐《黑雨》材料与微型遭遇的生产基线：内容包现含 28 件材料/工具、12 个遭遇；新增药灶灰、欠量石、锈雨染料、冷封蜡、守夜灯芯等材料，分别服务于配给、保管、守夜、迁徙与证词互证，避免以重复跑图虚增时长。
- 新增《黑雨》材料显形与互证链：昼盐浮尘、返潮绳结、囚雨芦、扶桑烬与人话盐晶通过雨棚场景获得“获得—显形—使用—代价”闭环，并以公开记录接入终局和第二卷。
- 新增《黑雨》村巷夜巡微型遭遇链：昼盐空碗、迟归影子与避雨门槛让隗青、青芽、砚伯和砚童在主线之外拥有可调查、可留样且会写入余响的日常冲突；新增材料“昼盐浮尘”。
- 补齐《黑雨》四条终局的后日回访：交羿、东行、折箭与北方交易各新增两节点，分别处理箭痕公开、渡口先行路线、迟日命名与迁徙木牌债务，使每种结局均有独立的可选社会后果与第二卷输入。
- 推进《黑雨》雨前村庄回访：新增粮种、避雨布、守夜灯与羊圈岔口的六节点支线，将照护、记录和迁徙的具体成本接入最终会议及卷末地点余响。
- 推进《黑雨》终局扩写：新增四方“不可交易之物”谈判，以杳湾的名字、盐泽的回头权、北方的署名责任与箭的问责条件进入取箭前的实际对白和结局分支。
- 推进《黑雨》旧盐井深层回访：新增家属优先核验、井水按病势分配、砚童自主署名与共同保管井钥匙的五节点支线，使历史证词与当夜生存不再被切成两段孤立剧情。
- 推进《黑雨》扩写计划第二轮的东桑十巢回访：新增阿禾与黎苇围绕慢行名单、被删去的快路、留守署名和“可离开”路标的五节点支线，使东桑选择继续影响终局与卷末余响。
- 开始《黑雨》扩写计划第二轮：新增“归还者号”雨中回访链，围绕渡口漏记名单、有限药物、倒潮证词和渡灯归属扩写宿沙、青芽、隗青、砚童与黎苇的对话冲突；新材料“返潮绳结”已接入旧盐井撤退、最终会议与卷末余响。
- 完成《黑雨》扩写计划批次 E 的第一阶段：新增北滩“门槛谈话”五节点及对应任务、事件/人物山海志条目，使儿童梦税、旧船蜡印、北方责任与撤离配给成为终局前可主动调查、可保留分歧的对话线；前序船舱、盐层与社会回访状态现已在最终会议中产生可读回响。

- 完成《黑雨》扩写计划批次 D 的第一阶段：新增终局前村庄回访网，强化青芽的配给、隗青的守夜与砚童的证词选择，使副本证据开始落到具体人物与社会后果。
- 完成《黑雨》扩写计划批次 C 的第一阶段：扩展旧盐井人话盐层、梦税名单室与暗渠救援，新增人话盐晶、证词分流与深层余响；现有含晦多解遭遇保持并获得更完整的前置证据与撤退条件。
- 完成《黑雨》扩写计划批次 B：深化东桑十巢的命名簿、鸟影、冷巢幻路与梦税缺页，使阿禾与黎苇的撤离立场冲突、梦壳处置与卷末余响进入内容接口。
- 完成《黑雨》扩写计划批次 A：将倒船腹舱深化为逆潮证词、儿童名牌、船底暗渠、水客记忆与返航结算构成的中型副本，并新增宿沙证词线、水客遭遇、材料显形、北方蜡印图鉴及卷末余响读取。
- 增加《黑雨》v1.3 扩写执行计划，将十小时目标拆分为倒船腹舱、东桑十巢、旧盐井、人物回访与终局余响五个可验证批次，并定义文本、节点、材料、接口、试玩与验收预算。
- 将《黑雨》第一轮“10小时结构”落为 21 个可运行扩展节点、5 条支线、3 个知识/资源微遭遇与 4 个山海志条目；覆盖井病、河床归还、鸟影归巢、旧井准备和终局前回访，并与任务、状态、材料、图鉴和卷末余响接口联动。
- 增加基于 Stitch 参考包的 UI 界面说明书，固化暗色古卷视觉、叙事日志、舆图、战斗记录、角色任务抽屉及其项目内容映射规范。
- 增加 `docs/ui-design` UI 设计交付目录及 750×1334 竖屏自适应详细任务书，统一上古神话神秘视觉、排版、组件、交互、无障碍与 Stitch 生成规范。
- 增加《黑雨》原创场景底图，并以黑雨、赤水、扶桑、十日未临与祭祀纹样重塑浏览器原型的太古神话视觉方向。
- 建立 `剧情/卷/章/{内容包,正文,资料,备份}` 归档体系；第一卷《黑雨》第一章已迁入，并冻结 `2026-08-13_v1.1.0` 完整章节备份。
- 增加《黑雨》10—12小时完整探索章节蓝图，定义主线、区域、副本、人物线、内容量与时长验收基线。
- 增加《黑雨》文学扩写版与文本结构研究说明，强化核心场景的环境、社会背景、角色行动、内心与对话。
- 增加浏览器系统原型：现代东方神话与古籍质感的桌面三栏界面，以及手机单栏与底部导航。
- 增加角色创建、资源状态、探索消耗、材料认知、山海志、装备、人物关系与世界状态交互沙盘；第一章正式剧情保持空白等待后续接入。
- 增加版本化本地自动/手动存档、JSON 导入导出、损坏校验、阅读与无障碍设置。
- 增加浏览器平台适配边界及微信小游戏迁移接口说明。
- 完成第一卷《黑雨》五幕、28 个可达剧情节点与四条卷末路线的完整游戏小说母稿。
- 增加角色、出身、任务、物品与材料显形、含晦多解遭遇、山海志分层和跨卷世界状态内容包。
- 增加 TypeScript 内容契约、剧情适配示例及 JSON 引用/节点可达性自动校验与小说稿生成工具。
- 建立仓库级开发前置规则，要求人工与 AI 开发在实施前完成需求、Git 状态和适用规范检查。
- 建立完整开发规范、Web/移动浏览器适配规范、微信小游戏迁移准备规范、开发与发布检查表及 ADR 模板。
- 增加平台抽象、触控与安全区、生命周期、存档迁移、资源预算、安全隐私和 AI 辅助开发约束。

### 修改

- 将浏览器原型替换为基于 Stitch 参考原包的暗色古卷界面壳：引入顶部书名栏、纵向叙事日志、三段角色状态栏、细金分隔、固定意图栏与移动端底部导航，同时保留既有剧情、选择、档案和存档行为。
- 统一 UI 设计交付目录命名为“项目_文档类型_范围或主题_版本”，同步任务书、Stitch 参考说明书及参考原包名称，并新增目录级命名与备份规则。
- 将手机优先、聊天式剧情、逐字展开、历史回看、四步角色创建、信息密度、阅读设置及微信小游戏安全区／生命周期／资源适配要求统一补充到两份 UI 规范的现状基线与验收项。
- 《黑雨》内容包升级至 v1.3.0；运行时、构建器、对白引号工具、读取说明和测试均支持基础节点与扩展节点的合并加载。
- 将已完成的手机优先、聊天式剧情、逐字展开、历史回看、三级档案、四步角色创建与减弱动效行为汇总进 UI 设计任务书，作为后续设计不得回退的现状基线。
- 重写第一卷《黑雨》关键场景，以人物质问、隐瞒、争论、交易和条件回应推进线索，减少说明式旁白。
- 更新项目本地预览、生产构建与测试入口说明。
- 强化 Git 分支、提交、标签、远程备份和定期恢复演练规则；明确本地提交与同步盘均不能替代异地备份。
- 更新项目管理入口，要求所有后续开发先阅读开发前置规则。

### 修复

- 修复 `codex.json` 中「退水河床」条目缺花括号导致与「归还者号」被 JSON 合并、退水河床内容丢失的问题；`codexEntries` 计数 32→33。
- 修复切换角色时剧情面板未重挂载导致逐字打字机计时器、进度与滚动观察器跨角色残留的问题：给面板加 `key={activeId}` 并在切换时清空面板滚动缓存。
- 清理仓库遗留项：将 `package.json` 版本号对齐为 `1.3.0`（与 `VERSION` 及 `CHANGELOG.md` 的 `[1.3.0]` 章节一致）；删除无源码引用的重复文件 `build/sites-vite-plugin.ts` 与死桩 `next.config.ts`；同步 `README.md` 与 `docs/development/ARCHITECTURE.md` 中第一卷《黑雨》第一章内容包 v3.0.0 已接入运行的现状描述。

### 移除

- 暂无。

## [1.3.0] - 2026-08-13

### 新增

- 建立《山海异闻录：天地未定》游戏设计说明书暨开发任务书 V1.3 基线。
- 补充需求治理、DoR/DoD、需求追踪矩阵和 Demo 范围分级。
- 补充性能、兼容、稳定性、无障碍、存档导入安全与隐私基线。
- 补充构建发布、遥测研究、资产授权、文化与内容安全治理。
- 建立 Git、版本号和修改日志管理规范。

### 修改

- 统一说明书正文、目录、页眉页脚与文件名版本口径。
- 规范个别口语化或存在歧义的设计表述。

### 验证

- Word 更新目录和字段后导出 28 页 PDF。
- 完成逐页视觉检查和 DOCX 结构/无障碍审计。

[未发布]: ./
[1.3.0]: ./
