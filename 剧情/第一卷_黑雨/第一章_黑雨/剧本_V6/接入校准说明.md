# 第一卷《黑雨》V6 剧本接入校准说明

来源：`第一卷_黑雨_剧本_引擎版.json`（V6.0）。目标内容包：`../内容包/`（v4.3.3）。

## 本次安全接入

- 两份数据均为 374 个节点、809 个选择；所有选择 ID 一一对应，节点标题与选项序号一致。
- 将 V6 新增的 9 个“不可逆”文字标记同步到玩家可见的选择标签，覆盖梦壳、含晦与卷末赤金箭抉择。
- `manifest.json` 登记 V6 引擎剧本为最新剧本文本来源，便于后续继续校准。

## 运行时保护

V6 中 754/809 个选择仍标记 `pendingCalibration`，其 `next` 为 `null`，并没有可执行效果；本内容包已有的 `next`、`effects`、条件、代价、稳定节点 ID、存档状态与跨卷回响全部保留。不可用 V6 文件直接覆盖内容包。

另外，V6 的少数差异是“条件台词/条件余响”占位、截断系统提示及版权说明，不属于玩家可运行文本，继续以内容包中已校准的条件块与系统记录为准。

## V6.1 开场重排与死循环修复（2026-08-25）

- 重排第一幕 001–009 的叙事铺排：001 立世界（时代/背景/环境），002 补世界规则与盐井之谜，003/004/006 各聚焦单事件（渡口宿沙、粮仓青芽、老井缄婆），005 放缓为先争论、后由隗青带出北滩浮尸；删除 001 无前文铺垫的「去粮仓问空屋」选项。
- 修复引擎 JSON 节点 `V01-ACT3_RIVER_REVERSED-020` 指向自身的死循环（choice `V01-C01-N041-E11` 的 next 由 `...-020` 改为 `...-021`）。
- 同步引擎 JSON 中 001–009 的 choice `label`/`stateWrite` 与重排后 md 文本一致；001 由 3 选项减为 2。
- **约束不变**：V6 仍属全卷待校准草稿，741/809 选择 `next=null`（本次仅修真实自环，未接其他跳转）；运行以 `../内容包/`（v4.3.3）为准，禁止用本 V6 文件覆盖内容包。
