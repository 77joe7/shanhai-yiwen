# 项目开发前置规则

本文件是《山海异闻录：天地未定》仓库的强制开发入口，适用于人工开发者、AI 编程代理和自动化工具。仓库根目录及所有子目录中的开发活动均受本文件约束。

## 开始任何工作前

1. 阅读 `README.md`、`VERSIONING.md`、`CHANGELOG.md` 和 `docs/development/DEVELOPMENT_STANDARD.md`。
2. 涉及浏览器、移动端或微信小游戏时，同时阅读：
   - `docs/development/WEB_MOBILE_STANDARD.md`
   - `docs/development/WECHAT_MINIGAME_READINESS.md`
3. 运行 `git status --short --branch`，识别当前分支、未提交修改和未跟踪文件。
4. 保留不属于当前任务的已有修改；不得覆盖、回退、删除或混入本次提交。
5. 确认需求满足 DoR；需求不明确且会影响产品行为、数据契约或平台方案时，先停止实现并澄清。
6. 按 `docs/development/DEVELOPMENT_CHECKLIST.md` 建立本次任务的验收与验证范围。

## 强制规则

- 游戏设计说明书及已批准的受控文档是产品需求基线；不得自行增删玩法、改动数值含义或扩大需求范围。
- 当前交付平台为桌面浏览器和手机浏览器；实现必须为后续微信小游戏迁移保留平台抽象边界。
- 业务代码不得直接散落调用 DOM、浏览器存储、浏览器生命周期或微信专有 API；必须经平台适配层访问。
- 不得硬编码屏幕尺寸、设备安全区、平台路径、密钥、账号、服务地址或可配置游戏数值。
- 每项实质修改必须更新 `CHANGELOG.md` 的“未发布”部分，并形成单一目的、可复核的 Git 提交。
- Git 提交不是完整备份：提交后必须推送到受控远程仓库才形成异地副本；未配置远程时必须明确报告“仅有本地版本记录”。
- 不得使用同步盘、编辑器历史、未提交工作区、压缩包或构建产物替代 Git 版本与远程备份。
- 禁止提交密钥、令牌、个人信息、生产配置、临时文件、缓存、日志及未授权资产。
- 未完成与改动风险相匹配的验证，不得声称任务完成；无法运行的检查必须明确记录原因和风险。
- 未经明确授权，不得执行破坏性 Git 操作、覆盖已发布文件、改写共享历史或删除用户修改。

## 完成与交付

任务只有在以下条件全部满足时才算完成：实现符合需求、相关测试通过、平台适配已检查、文档与变更日志同步、差异经人工或代理复核、工作区状态已说明，并且按约定完成 Git 提交与远程备份。详细 DoD 见 `docs/development/DEVELOPMENT_STANDARD.md`。

如本文件与更具体的受控规范冲突，按“用户当前明确指令 > 已批准产品需求 > 本文件 > 专项规范 > 一般惯例”的顺序处理，并在变更记录中说明例外。
